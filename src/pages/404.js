import React from 'react'
import Layout from '../components/layout'

const NotFoundPage = () => (
  <Layout>
    <h1>No disponible</h1>
    <p>La página que estás buscando no existe. Regresa a la página de inicio y vuelve a intentarlo, por favor.</p>
  </Layout>
)

export default NotFoundPage

